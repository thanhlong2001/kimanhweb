<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DECROO</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Grand+Hotel&display=swap" rel="stylesheet">
  <style>
    body, html {
      height: 100%;
      margin: 0;
      font-family: Arial, sans-serif;
      background: linear-gradient(45deg, #ff9aa2, #ffdac1); /* Gradient màu kẹo */
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .login-container {
      background-color: #fff7e6; /* Màu nền dịu nhẹ */
      border: 1px solid #f5e1da;
      padding: 40px;
      width: 350px;
      border-radius: 16px; /* Viền bo tròn mềm mại hơn */
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      text-align: center;
    }
    .login-container h1 {
      font-family: 'Grand Hotel', cursive; /* Font chữ mềm mại */
      font-size: 48px;
      color: #ff6b6b; /* Màu đỏ nhạt cho tiêu đề */
      margin-bottom: 20px;
    }
    .login-container form {
      width: 100%;
    }
    .login-container .form-control {
      margin-bottom: 15px;
      border-radius: 10px; /* Viền input bo tròn hơn */
      border: 1px solid #f0c9c9;
      background-color: #fffaf3; /* Màu nền dịu nhẹ cho input */
    }
    .login-container .btn-primary {
      background-color: #ff6b6b; /* Màu đỏ ngọt ngào */
      border-color: #ff6b6b;
      width: 100%;
      padding: 10px;
      font-size: 16px;
      border-radius: 8px;
      color: #fff; /* Màu chữ trắng */
    }
    .login-container .btn-primary:hover {
      background-color: #ff3d3d; /* Màu đỏ đậm hơn khi hover */
      border-color: #ff3d3d;
    }
    .login-container .btn-secondary {
      background-color: #ffd166; /* Màu vàng ngọt ngào cho nút "Quên mật khẩu?" */
      color: #ffffff;
      font-size: 14px;
      margin-top: 10px;
      padding: 10px;
      width: 100%;
      border-radius: 8px;
      display: block;
      text-align: center;
      text-decoration: none;
    }
    .login-container .btn-secondary:hover {
      background-color: #f4a261; /* Màu vàng đậm hơn khi hover */
    }
    .divider {
      display: flex;
      align-items: center;
      margin: 20px 0;
    }
    .divider::before, .divider::after {
      content: "";
      flex: 1;
      height: 1px;
      background: #ff6b6b;
    }
    .divider::before {
      margin-right: 10px;
    }
    .divider::after {
      margin-left: 10px;
    }
    .login-container a {
      color: #ff6b6b;
      text-decoration: none;
    }
    .login-container a:hover {
      text-decoration: underline;
    }
    .login-container p {
      font-size: 14px;
      color: #555;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>DECROO</h1>
    <form action="/login" method="POST">
      <input type="text" class="form-control" id="username" name="username" placeholder="Email hoặc số điện thoại" required>
      <input type="password" class="form-control" id="password" name="password" placeholder="Mật khẩu" required>
      <button type="submit" class="btn btn-primary">Đăng nhập</button>
      <a href="#" class="btn btn-secondary">Quên mật khẩu?</a>
    </form>
    <div class="divider">HOẶC</div>
    <p>Bạn chưa có tài khoản? <a href="/register">Đăng ký ngay</a></p>
  </div>
</body>
</html>
