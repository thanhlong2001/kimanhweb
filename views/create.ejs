<!DOCTYPE html>
<html lang="en">
<head>
  <title>Biểu mẫu tạo sản phẩm - The Zdee</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Grand+Hotel&display=swap" rel="stylesheet">
  <style>
    body, html {
      height: 100%;
      margin: 0;
      font-family: Arial, sans-serif;
      background: linear-gradient(45deg, #ff9aa2, #ffdac1);
      display: flex;
      justify-content: center;
      align-items: center; /* Căn giữa theo chiều dọc */
    }
    .container {
      background-color: #ffffff;
      border: 1px solid #f5e1da;
      padding: 40px;
      width: 600px;
      border-radius: 16px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      margin-top: 20px;
    }
    h2 {
      font-family: 'Grand Hotel', cursive;
      font-size: 36px;
      color: #ff6b6b;
      text-align: center;
      margin-bottom: 20px;
    }
    .btn-primary {
      background-color: #ff6b6b;
      border-color: #ff6b6b;
    }
    .btn-primary:hover {
      background-color: #ff3d3d;
      border-color: #ff3d3d;
    }
    .form-control {
      border-radius: 8px;
      box-shadow: none; /* Xóa bóng cho ô nhập liệu */
      transition: border-color 0.3s;
    }
    .form-control:focus {
      border-color: #ff6b6b; /* Đổi màu viền khi ô nhập liệu được chọn */
      box-shadow: 0 0 5px rgba(255, 107, 107, 0.5);
    }
  </style>
</head>
<body>

<div class="container mt-3">
  <h2>Biểu mẫu tạo sản phẩm</h2>
  <form action="/create" method="post" enctype="multipart/form-data"> <!-- Thêm enctype để hỗ trợ tải tệp -->
    <div class="mb-3 mt-3">
      <label for="name">Tên:</label>
      <input type="text" class="form-control" id="name" 
        placeholder="Nhập tên" name="name" required>
    </div>
    <div class="mb-3 mt-3">
      <label for="price">Giá:</label>
      <input type="number" class="form-control" id="price" 
        placeholder="Nhập giá" name="price" required>
    </div>
    <div class="mb-3 mt-3">
      <label for="quantity">Số lượng:</label>
      <input type="number" class="form-control" id="quantity" 
        placeholder="Nhập số lượng" name="quantity" required>
    </div>
    <div class="mb-3 mt-3">
      <label for="brand">Thương hiệu:</label>
      <input type="text" class="form-control" id="brand" 
        placeholder="Nhập thương hiệu" name="brand" required>
    </div>
    <div class="mb-3 mt-3">
      <label for="image">Hình ảnh:</label>
      <input type="file" class="form-control" id="image" 
        name="image" accept="image/*" required> <!-- Thêm trường chọn file hình ảnh -->
    </div>
    <button type="submit" class="btn btn-primary">Thêm sản phẩm</button>
  </form>
</div>

</body>
</html>
